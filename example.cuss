enum Color
    Red
    Green
    Blue

struct Point
    x: int
    y: int

pub fn minus_and_square(a: int, b: int) -> int
    const diff = a - b
    const square = diff * diff
    if square > 100
        return square
    else
        return 0
